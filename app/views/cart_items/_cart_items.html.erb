<%= turbo_stream_from cart_items, dom_id(cart) %>
<div id=<%= dom_id(cart) %>>
  <% cart_items.each do |cart_item| %>
    <%= render "cart_items/cart_item", cart_item: cart_item %>
  <% end %>
</div>
